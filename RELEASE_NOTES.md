## TextRestore v1.8.0

检测并还原 AI 模型（ChatGPT / Claude / Gemini 等）输出中被偷偷替换的**隐蔽字符**——全角字符、零宽字符、花体字母、西里尔相似字母等，用于识别中文用户的字符替换。

### v1.8.0 更新

- **记忆输入状态**：程序退出后保留上次的输入内容，下次启动自动恢复（含光标位置），输出框随恢复内容自动重新转换
- 保存策略：停止输入 **600ms 后自动落盘**（防抖），**关闭窗口时立即保存**，清空后退出同样会被记住
- 输入状态存入 `%APPDATA%\TextRestore\config.json`（`last_input` / `last_cursor`），与设置共用配置文件

### 特性

- 上输入 / 下输出双文本框，输入即自动转换
- **记忆输入状态**：重启自动恢复上次输入内容与光标
- diff 高亮：输入红 / 输出绿；选中联动浅蓝高亮
- 滚动同步（滚轮/滚动条/键盘/拖拽）；窗口缩放自适应
- 现代设置页：左侧锚点导航 + 统一滚动内容区（scrollspy）；多行前后缀；深色主题；平滑 iOS 开关
- 字体与颜色自定义（输入/输出框独立配置）；中/英双语 UI；新图标
- 11 类还原，均在设置中独立开关：
  1. 全角转半角：`ＡＢＣ１２３` → `ABC123`
  2. 中文标点转英文：`，。？！《》“”` → `,.?!<>""`
  3. 花体/数学字体：`𝐀𝐁𝐂𝟏𝟐𝟑` → `ABC123`
  4. 上下标：`x²y₃` → `x2y3`
  5. 相似字母（西里尔/希腊/小型大写）：`СА ᴀᴋ` → `CA AK`
  6. 删除不可见字符：零宽空格/连接符/BOM/变体选择符/双向控制符/标签字符等
  7. 特殊空格归一
  8. 日期时间格式：`2024年1月5日下午3点` → `2024-01-05 15:00`
  9. AI 提示词标记：`<|im_start|>` `[INST]` 等 → 删除
  10. 移除 AI 署名注释（默认关闭）
- 自动删除末尾换行（可关闭），一键复制，全设置持久化
- Windows 单文件 exe，无需安装 Python，无控制台窗口

### 使用

下载 `TextRestore.exe` 双击运行即可。

---

### v1.7.2

- AI 提示词标记清理 + AI 署名注释移除（默认关闭）+ 语言标签 U+E0000

### v1.7.1

- 补齐隐蔽字符类别（变体选择符/隔离控制符/盲文/标签块/行分隔符/标点变体等）

### v1.7.0

- 日期时间格式还原：中文日期时间 → 标准英文格式（ISO 8601）

### v1.6.1

- 修复暗色主题下拉框文字看不清；修复窗口图标未生效（标题栏羽毛 → 应用图标）

### v1.6.0

- 全英文 UI（中/英双语，自动检测 + 手动切换 + 持久化）；新图标（渐变方块 + 红绿分色 A）

### v1.5.2

- 设置页：左侧锚点导航 + 统一滚动内容区（scrollspy 反向同步选中）

### v1.5.1

- 设置窗口左侧导航 + 统一滚动内容区；修复按钮被压缩问题

### v1.5.0

- 字体与颜色自定义：输入/输出框分别设置字体、字号、替换字符与高亮的前景/背景色

### v1.4.1

- 开关边缘超采样抗锯齿平滑

### v1.4.0

- iOS 风格开关替换传统勾选框

### v1.3.0

- 设置面板收纳还原选项；多行前后缀；信息提示独占一行；修复跨框选区残留；全设置持久化

### v1.2.0

- 选中联动（IDE diff 效果）+ 浅蓝选中色；拖拽选中滚动同步；深色主题

### v1.1.0

- 修复窗口缩放布局；滚动同步；删除末尾换行

### v1.0.0

- 初始版本：上下双文本框 + 自动转换 + 一键复制 + diff 高亮
