## TextRestore v1.3.0

检测并还原 AI 模型（ChatGPT / Claude / Gemini 等）输出中被偷偷替换的**隐蔽字符**——全角字符、零宽字符、花体字母、西里尔相似字母等，用于识别中文用户的字符替换。

### v1.3.0 更新

- **设置面板**：还原选项勾选框从两个文本框中间移入"设置"窗口（主界面更简洁），主界面右下角"设置"按钮打开
- **前后缀功能**：设置中提供两个多行文本框分别配置前缀/后缀（支持换行，如用 ```` ``` ```` 代码块包裹输出）；主界面新增"包含前缀/包含后缀"勾选框，勾选后附加到输出结果，可实时预览
- **信息提示独占一行**：状态栏消息移到输出框下方独立成行，不再与按钮挤在同一行
- **修复跨框选区的旧高亮残留**：在 A 框选中后转到 B 框选中时，A 框原来的选中高亮会被清除，B 框的对应区域在 A 框中以镜像高亮显示（顺带修复了程序化清除选区触发 `<<Selection>>` 递归回环的问题）
- **全设置持久化**：主题、还原选项、前后缀内容、包含开关全部保存到 `%APPDATA%\TextRestore\config.json`，重启自动恢复
- 前缀存在时，diff 高亮与选中联动的坐标映射同步偏移，镜像选择仍然精确

### 特性

- 上输入 / 下输出双文本框，输入即自动转换
- diff 高亮：输入红 / 输出绿；选中联动浅蓝高亮
- 滚动同步（滚轮/滚动条/键盘/拖拽）；窗口缩放自适应
- 设置面板 + 多行前后缀 + 深色主题
- 7 类还原，均在设置中独立开关：
  1. 全角转半角：`ＡＢＣ１２３` → `ABC123`
  2. 中文标点转英文：`，。？！《》“”` → `,.?!<>""`
  3. 花体/数学字体：`𝐀𝐁𝐂𝟏𝟐𝟑` → `ABC123`
  4. 上下标：`x²y₃` → `x2y3`
  5. 相似字母（西里尔/希腊/小型大写）：`СА ᴀᴋ` → `CA AK`
  6. 删除不可见字符：零宽空格/连接符/BOM/双向控制符等
  7. 特殊空格归一
- 自动删除末尾换行（可关闭），一键复制
- Windows 单文件 exe，无需安装 Python，无控制台窗口

### 使用

下载 `TextRestore.exe` 双击运行即可。

---

### v1.2.0

- 选中联动（IDE diff 效果）+ 浅蓝选中色
- 拖拽选中滚动同步；深色主题

### v1.1.0

- 修复窗口缩放布局；滚动同步；删除末尾换行

### v1.0.0

- 初始版本：上下双文本框 + 自动转换 + 一键复制 + diff 高亮
