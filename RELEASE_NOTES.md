## TextRestore v1.1.0

检测并还原 AI 模型（ChatGPT / Claude / Gemini 等）输出中被偷偷替换的**隐蔽字符**——全角字符、零宽字符、花体字母、西里尔相似字母等，用于识别中文用户的字符替换。

### v1.1.0 更新

- **diff 高亮**：输入框中被替换的字符飘红，输出框中还原后的字符飘绿（v1.0.0 已引入，本次修复了末尾换行区间与相邻区间合并的一个 bug）
- **修复窗口缩放布局**：按钮不再被压扁或消失；两个文本框等高均匀分配空间，缩放行为一致；选项区改为两行布局，窄窗口下也不会裁剪
- **同步滚动**：输入/输出框滚动位置保持一致（滚轮、滚动条、键盘翻页均可）
- **删除末尾换行**：输入末尾多余的 `\n` / `\r` 自动从输出中删除，并在输入框中飘红标注（可在选项中关闭）

### 特性

- 上输入 / 下输出双文本框，输入即自动转换
- 7 类还原，均可独立开关：
  1. 全角转半角：`ＡＢＣ１２３` → `ABC123`
  2. 中文标点转英文：`，。？！《》“”` → `,.?!<>""`
  3. 花体/数学字体：`𝐀𝐁𝐂𝟏𝟐𝟑` → `ABC123`
  4. 上下标：`x²y₃` → `x2y3`
  5. 相似字母（西里尔/希腊/小型大写）：`СА ᴀᴋ` → `CA AK`
  6. 删除不可见字符：零宽空格/连接符/BOM/双向控制符等
  7. 特殊空格归一
- 一键复制结果，状态栏统计各类还原数量
- Windows 单文件 exe，无需安装 Python，无控制台窗口

### 使用

下载 `TextRestore.exe` 双击运行即可。

---

### v1.0.0

- 初始版本：上下双文本框 + 自动转换 + 一键复制
- diff 高亮（输入红 / 输出绿）
