## TextRestore v1.2.0

检测并还原 AI 模型（ChatGPT / Claude / Gemini 等）输出中被偷偷替换的**隐蔽字符**——全角字符、零宽字符、花体字母、西里尔相似字母等，用于识别中文用户的字符替换。

### v1.2.0 更新

- **选中联动（IDE diff 效果）**：鼠标框选输入框或输出框中的任意内容，另一边的对应区域同步高亮；选中背景改为自定义**浅蓝色**（替代系统默认蓝），无论框选哪一边，两个文本框对应区域一起高亮
- **修复拖拽选中时的滚动同步**：鼠标拖拽框选触发文本自动滚动时，另一个文本框现在会跟随滚动
- **深色主题**：右下角"深色主题/浅色主题"按钮一键切换，选择自动保存（`%APPDATA%\TextRestore\config.json`），重启后保留
- 选区与 diff 标记的优先级已处理：联动高亮盖过红/绿标记，选区永远可见

### 特性

- 上输入 / 下输出双文本框，输入即自动转换
- diff 高亮：输入红 / 输出绿；选中联动浅蓝高亮
- 滚动同步：滚轮、滚动条、键盘翻页、拖拽自动滚动
- 深色主题一键切换
- 7 类还原，均可独立开关：
  1. 全角转半角：`ＡＢＣ１２３` → `ABC123`
  2. 中文标点转英文：`，。？！《》“”` → `,.?!<>""`
  3. 花体/数学字体：`𝐀𝐁𝐂𝟏𝟐𝟑` → `ABC123`
  4. 上下标：`x²y₃` → `x2y3`
  5. 相似字母（西里尔/希腊/小型大写）：`СА ᴀᴋ` → `CA AK`
  6. 删除不可见字符：零宽空格/连接符/BOM/双向控制符等
  7. 特殊空格归一
- 自动删除末尾换行（可关闭），一键复制，状态栏统计
- Windows 单文件 exe，无需安装 Python，无控制台窗口

### 使用

下载 `TextRestore.exe` 双击运行即可。

---

### v1.1.0

- 修复窗口缩放布局（按钮不再被压扁/消失，两文本框等高）
- 输入/输出滚动位置同步
- 删除末尾换行并在输入框标注

### v1.0.0

- 初始版本：上下双文本框 + 自动转换 + 一键复制 + diff 高亮
